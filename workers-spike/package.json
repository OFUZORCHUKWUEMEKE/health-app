{
  "name": "workers-spike",
  "private": true,
  "version": "0.0.0",
  "description": "Phase 0 / Spike 1 — does the mongodb driver survive workerd's node:net/node:tls shim? Throwaway, not part of the main app build.",
  "scripts": {
    "dev": "wrangler dev",
    "bundle-check": "wrangler deploy --dry-run --outdir dist",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.31.1",
    "@cloudflare/workers-types": "^5.20260911.1",
    "typescript": "^5.1.3"
  },
  "dependencies": {
    "mongodb": "6.13.1"
  }
}
